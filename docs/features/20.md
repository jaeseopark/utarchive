# PR 20 — Final Polish & Deployment Verification

**Phase:** 6 — Search & Polish  
**Depends on:** all previous PRs

## Goal

Verify the full end-to-end stack in Docker, audit dependencies, and produce the README and environment variable documentation.

## Scope

**In:**
- `README.md` with setup instructions, env var reference, and development workflow
- Dockerfile smoke test: build + run + hit `/health`
- Dependency audit: confirm all packages are at their latest semver-compatible versions
- `.nvmrc` verification against current Node LTS
- Fix any integration issues discovered during end-to-end testing
- `docker-compose.yml` final review (volume mounts for audio files, correct env var wiring)

**Out:**
- No new features
- No CI/CD pipeline (can be added as a follow-up)

## README Sections

1. **Overview** — what utarchive is
2. **Prerequisites** — Node (see `.nvmrc`), Docker, Postgres (external)
3. **Quick Start (Docker)** — build image, set env vars, run container
4. **Development Setup** — `nvm use`, `npm install`, `drizzle-kit push`, `npm run dev` per workspace
5. **Environment Variables** — table of all vars with descriptions and whether they are required
6. **Audio File Storage** — explanation of `file_path`, recommended Docker volume mount path
7. **TOTP Setup** — how to generate a `totp_key` and configure an authenticator app
8. **Database Migrations** — how to run `drizzle-kit generate` and `drizzle-kit push`

## Environment Variables Reference

| Variable | Required | Description |
|---|---|---|
| `DATABASE_URL` | ✅ | Postgres connection string |
| `AUTH_CREDENTIALS` | ✅ | `id,password,totp_key` (comma-separated) |
| `JWT_SECRET` | ✅ | Random secret for JWT signing (min 32 chars recommended) |
| `JWT_TTL_SECONDS` | — | JWT lifetime in seconds (default: 3600) |
| `PORT` | — | HTTP server port (default: 3000) |
| `NODE_ENV` | — | `production` or `development` (default: `development`) |

## Docker Volume for Audio Files

The `file_path` column stores absolute paths inside the container. Recommended convention:

```
docker run \
  -v /host/music:/music \
  --env-file .env \
  utarchive
```

Store `file_path` values as `/music/artist/song.mp3` etc.

## Acceptance Criteria

- [ ] `docker build -t utarchive .` completes without errors on a clean checkout
- [ ] `docker run --env-file .env utarchive` starts the server, `GET /health` returns 200
- [ ] React frontend is served at `/` from inside the container
- [ ] `npm audit` reports no high or critical severity vulnerabilities
- [ ] `npm outdated` in each workspace shows no packages more than one major version behind
- [ ] README accurately describes all env vars and the Docker volume convention
- [ ] `drizzle-kit push` against a fresh Postgres instance applies all migrations without errors
